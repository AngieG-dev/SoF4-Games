import { BrowserRouter, Routes } from 'react-router-dom' // Agregar Route cuando se necesite

function App() {
  return (
      <BrowserRouter>
          <Routes>
              {/* Las rutas se agregan aquí en sprints siguientes */}
          </Routes>
      </BrowserRouter>
  )
}

export default App
